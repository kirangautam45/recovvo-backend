/**
 * List of numeric user types.
 */
export enum NumericUserTypes {
  Admin = 1,
  User,
  Supervisor
}

/**
 * List of string user types.
 */
export enum UserTypes {
  ADMIN = 'Admin',
  USER = 'User',
  SUPERVISOR = 'Supervisor'
}
